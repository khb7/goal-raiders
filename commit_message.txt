feat: Implement sub-boss functionality and fix frontend issues

- Backend:
  - Modified Goal, GoalController, GoalRepository for potential future sub-boss integration (though not directly used in this commit).
  - Removed HelloController.
- Frontend:
  - Added sub-boss functionality with parentId for bosses.
  - Implemented hierarchical display of bosses in the UI.
  - Fixed multiple `no-undef` and syntax errors in App.js and index.js.
  - Cleaned up redundant "New Game" button.